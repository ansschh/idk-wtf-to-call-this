specVersion: "v1"
serviceId: "edith"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "edith-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
